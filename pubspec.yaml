name: portfolio
description: "My Personal Portfolio"
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  nb_utils: ^7.0.0
  flutter_riverpod: ^2.5.1
  core:
    git:
      url: https://github.com/dev-liber/core.git
      ref: main
  go_router: ^13.2.4
  url_launcher: ^6.3.0
  carousel_slider: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    

